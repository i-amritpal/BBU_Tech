import 'package:flutter/material.dart';

class Syllabus extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Syllabus Checked")),
    );
  }
}
